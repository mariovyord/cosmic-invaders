export const PLAYER_SPEED = 7;
export const PLAYER_ROTATION = 0.15;
export const PROJECTILE_SPEED = -15;
export const IMAGE_WIDTH = 30;
export const IMAGE_HEIGHT = 30;
// The desired number of frames per second (fps) at which the game should run
export const FRAMERATE = 60;
// Represents the time interval between each frame in milliseconds
export const FPS_INTERVAL = 1000 / FRAMERATE;
